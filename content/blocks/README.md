# Shared copy blocks

These paragraphs are rendered on the site AND pasted into every Etsy listing (the four mandatory
closing blocks + OUR PROMISE, from `etsy/SEO/LISTING-COPY-PACK.md`). Edit here first, then update
the listings, so the wording never diverges between channels. Read at build time by `lib/blocks.ts`.
